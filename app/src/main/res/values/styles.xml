<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorBlue</item>
        <item name="colorPrimaryDark">@color/colorPrimary</item>
        <item name="colorAccent">@color/colorRed</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="NormalStaticText">
        <item name="android:textSize">18sp</item>
        <item name="android:textColor">@color/colorBlack</item>
    </style>

    <style name="BigStaticText">
        <item name="android:textSize">26sp</item>
        <item name="android:gravity">center</item>
        <item name="android:textColor">@color/colorBlue</item>
    </style>

    <style name="MainBottomState">
        <item name="android:state_checked">true</item>
        <item name="android:textColor">@drawable/main_bottom_home_text_selector</item>
    </style>

    <style name="FullScreen">
        <item name="android:windowFullscreen">true</item>
    </style>
</resources>
